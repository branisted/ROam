{
  "name": "ROam-demo",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start-demo": "concurrently \"npm run demo --prefix backend\" \"npm run preview --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}